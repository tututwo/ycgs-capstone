# Reports

`initial_EDA.pdf` is the current rendered report generated from `analysis/initial_EDA.qmd`.

To regenerate it, install the environment and run:

```bash
Rscript scripts/render_report.R
```

The project-level `_quarto.yml` sends rendered outputs to this folder.
